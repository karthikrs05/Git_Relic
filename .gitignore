# dependencies
node_modules/

# build outputs
dist/
.vite/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# environment files
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.vscode/
.idea/

# local runtime data
server/data/*.local.json

# runtime server directories (created on first upload)
temp/
uploads/

# runtime flat-file data (created on first register/login)
server/data/users.json
server/data/accounts.json

# gitleaks binary (manually downloaded — not committed to repo)
gitleaks
gitleaks.exe
